rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Chỉ user đã đăng nhập mới đọc/ghi mọi collection
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
